# Example environment variables for Railway deployment
# Copy this to .env and set real values in Railway Dashboard or local .env
MONGODB_URI=mongodb://username:password@host:27017/your_db_name
UPLOAD_DIR=/tmp/uploads
WHISPER_MODEL=base
# Optional: If ffmpeg is installed in a custom location, set this.
# On Railway/container environments, ffmpeg may already be available at /usr/bin/ffmpeg
FFMPEG_DIR=/usr/bin
